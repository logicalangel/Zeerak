# Fedora COPR build settings.
# Used by `copr-cli build-package` and the Packit / COPR webhook integration.

[zeerak]
spec_file = deploy/copr/zeerak.spec
chroots = fedora-40-x86_64 fedora-40-aarch64 fedora-41-x86_64 fedora-41-aarch64 epel-9-x86_64
